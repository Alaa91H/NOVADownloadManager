# Aggressive Capture Mode

Aggressive Capture Mode is a capture-only feature that requests browser all-sites access (`<all_urls>`) for deeper discovery than the default conservative profile. It does not bypass protected media. Direct handoff still blocks `blob:` and `data:` URLs. No cookies, no Authorization headers, and no hidden telemetry.
